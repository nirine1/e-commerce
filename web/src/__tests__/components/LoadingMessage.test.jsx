import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import LoadingMessage from '../../components/LoadingMessage'

describe('LoadingMessage Component', () => {
    // it('devrait afficher le message de chargement', () => {
    //     // Render LoadingMessage avec un message
    //     // Vérifier que le message s'affiche
    // })

    // it('devrait afficher le spinner', () => {
    //     // Render LoadingMessage
    //     // Vérifier que le Spinner est présent (par rôle ou classe)
    // })

    // it('devrait appliquer les bonnes classes CSS au conteneur', () => {
    //     // Render LoadingMessage
    //     // Vérifier les classes: flex, items-center, justify-center, p-4
    // })

    // it('devrait appliquer les bonnes classes au spinner', () => {
    //     // Render LoadingMessage
    //     // Vérifier les classes du spinner: mr-2, h-4, w-4, text-blue-500
    // })
})
